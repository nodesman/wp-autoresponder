

1. Need to remove existing statements that are being used for routing non-administrator requests based on get variables to a file or a function or a page and then replace it with a mechanism (which can be created by extending the features of routing mechanism created for the administration section. 
2. Set up the development environment for running sonar, js-test-runner and jscoverage locally as part of the build. 
3. Task the autoresponder milestone.
